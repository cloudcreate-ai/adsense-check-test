# AI API key (optional — AI tests skip if not set)
# AI_API_KEY=your-key-here

# Skip integration tests that require Playwright + real network
# Set to empty string to enable: SKIP_INTEGRATION=
SKIP_INTEGRATION=1

# Test site URLs for integration tests
# TEST_SITE_URL=https://example.com
